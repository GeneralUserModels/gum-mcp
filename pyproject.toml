[project]
name = "gum-mcp"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "dotenv>=0.9.9",
    "gum",
    "mcp[cli]>=1.8.1",
]

[tool.uv.sources]
gum = { git = "https://github.com/GeneralUserModels/gum/" }
